get:
  summary: Get all Address By User Id
  tags:
    - Address
  responses:
    '200':
      description: List of Address
      content:
        application/json:
          schema:
            type: array
            address:
              $ref: '../../_schemas.yaml#/components/UpdateAddressReq'
    '500':
      description: Server error
